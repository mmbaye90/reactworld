import React from "react";
import logoreact from "../assets/logoreact.png"
const Logo=()=>{
    return(
        <div>
            <img src={logoreact} alt={logoreact} />
        </div>
    )
}
export default Logo